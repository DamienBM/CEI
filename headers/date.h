#pragma once

#include <librairie.h>

bool is_year_bissextile (const unsigned int&);
int date_correcte (int, int, int);
void saisie_date (int*, int*, int*);
long jours_ecoules (int, int, int);
void jour_semaine (long);
